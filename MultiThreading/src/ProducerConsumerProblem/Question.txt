Two Threads a producer and consumer Thread both shares a common ,fixed size buffer as queue,
The producer job is to generate data and put into buffer ,and the consumer job is to consume the data from the buffer queue,
The problem is to make sure that the producer won;t produce the data if the buffer is full
and the consumer won't consume the data is the buffer is empty.